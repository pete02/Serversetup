entryPoints:
  web:
    address: ":80"
  websecure:
    address: ":443"

providers:
  docker:
    exposedByDefault: false

api:
  dashboard: true
  insecure: true  # Change to false and route properly in prod

certificatesResolvers:
  letsencrypt:
    acme:
      email: lumilukko@gmail.com
      storage: /letsencrypt/acme.json
      httpChallenge:
        entryPoint: web
